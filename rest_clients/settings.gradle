rootProject.name = 'rest_clients'

include 'rest_client',
        'web_client',
        'rest_template',
        'http_interface',
        'open_feign_client',
        'open_feign_client:introduction_to_open_feign'
